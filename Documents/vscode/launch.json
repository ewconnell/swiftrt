// .vscode/launch.json
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "lldb Launch",
            "type": "lldb",
            "request": "launch",
            // Resolved by CMake Tools:
            "program": "${workspaceFolder}/.build/debug/SwiftRTPackageTests.xctest",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "swift-build-tests"
        }
    ]
}